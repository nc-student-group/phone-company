package com.phonecompany.model.enums.interfaces;

/**
 * Interface being implemented by all the entities that
 * can be persisted in the storage (enums as well).
 */
public interface Storable {
    Long getId();
}
